# Copy to .env and uncomment what you need. Values shown are the
# defaults from config.py. Real environment variables win over .env.

# API base URL the MCP server calls
#POLICY_API_URL=http://127.0.0.1:8004

# Per-request timeout, seconds
#POLICY_API_TIMEOUT=10

# How main.py listens: stdio | http | sse | streamable-http
#MCP_TRANSPORT=streamable-http

# Bind address for the MCP server
#MCP_HOST=0.0.0.0

# Port for the MCP server (must differ from the API's 8004)
#MCP_PORT=8104
